text
install
cdrom
lang en_US.UTF-8
keyboard us
timezone UTC --isUtc
network --bootproto=dhcp
rootpw --plaintext packer
firewall --enabled --ssh
user --name=ec2-user --groups=wheel --plaintext --password packer
selinux --enforcing
bootloader --location=mbr
autopart --type=lvm

%packages
@^minimal-environment
@core
chrony
cloud-init
%end

reboot
